# 当前状态

## 已完成

- 建立 `ledger-domain`、`ledger-application` 与 `ledger-data` 三个可构建的 Kotlin Multiplatform 共享库模块；账务核心继续保持无 Android、网络、同步或 AI 依赖。
- SQLDelight schema 当前为 v31（v1→v31 共 30 个迁移文件）。v10 以 dedicated normalized owners 保存 RG-06 与 RG-07 operation、relation/lifecycle/installment、source/evidence/candidate 与 reconciliation；v11 保存 RG-06 candidate confirmation provenance；v12 保存 D-082 批准的 RG-09 normalized owners；v13 保存 D-083 批准的 RG-10 normalized owners；v14 从 RG-09 current adjustment owner 移除由 immutable original delta、allocations 与 latest history 推导的重复金额/状态列；v15 保存 D-084 批准的 RG-08 lending owners 并接入 `LEND`/`COLLECT` canonical kinds；v16 保存 D-085 批准的 RG-11 owners 并接入 `PREPAID_PURCHASE`/`PREPAID_RECOGNITION` canonical kinds（含共享 `appendVersion` 与 `transaction_version.confirmation_id`）；v17 保存 RG-12 posting-facts correction owners，并把 confirmation guard 扩展到 `EXPENSE`；v18 保存 D-087 批准的 RG-02 `category_rename` append-only name history（17.sqm）；v19 为 D-088 的 RG-12 guard 修复新边 18.sqm（重挂 rg12 guard trigger，纯 DDL）；v20 为 DATA-001（D-091）的 schema 统一新边 19.sqm（共享 `formal_transaction_metadata` 表，RG-08/09/10 私表精简为 source-only、RG-11/12 私表删除）；v21 为 P4-02（D-098）的共享导入 spine 新边 20.sqm（9 张非 rgXX_ `import_*` 表与不可变/追加-only/状态迁移守卫触发器）；v22 落地 P4-04 transfer formalization；v23 按 D-103 增加 P4-08 evidence link、posting reconciliation 及其 append-only history；v24 按 D-104/D-105 增加 P4-07 duplicate candidate/status history/review claim/snapshot/receipt 五张 append-only 表，并为 `import_source_record` 追加 funding 列与 creation FK（重建表、`defer_foreign_keys` 保留全部 v23 import 与 P4-08 后代）；v25 按 D-107 增加 P4-06 片 1 的多腿 decision snapshot 扩展（`credit_liability_account_id`/`asset_account_id`、退款关联 `original_transaction_id` FK 与两腿金额列）、信用/混合 candidate kind 与 `import_candidate_payment_profile`、`mixed_payment` 关联组两表（建而不用，完成性与腿先头后写序触发器；重建 `import_source_record`/`import_candidate` 收录三个 contract_version=3 kind）；v26 按 D-112 增加 P4-08 evidence projection 新边 25.sqm（独立追加式 evidence_projection 表与守卫触发器；重建 reconciliation_request_snapshot/evidence_link 并把两处 basis_version CHECK 放宽为 {1,2} 追加六快照列；从已链接 precision=2 posting 确定性回填 READY projection，未匹配零行；带迁移前 fail-closed 数据守卫与回填后注入钩子）；v27 按 D-113 增加 P4-08 correction/successor invalidation 新边 26.sqm（激活预留 `invalidate_link` 操作与 `invalidated`/`posting_replaced` 失效事件 reason；`evidence_link` 支持 successor link 同事务替换并保持单 active；`posting_reconciliation` 结果态激活 `MISSING`/`DIFFERENCE`（写入者仅 correction port）；`evidence_projection` 增 `superseded_by_projection_id` 受控 supersede 面（单列+部分唯一索引+唯一合法转移触发器）；新表 `reconciliation_correction_snapshot`（双 guard）；零回填，带迁移前 fail-closed 数据守卫与回填后注入钩子）；v28 按 D-143 增加 P7-01 目录管理新边 27.sqm（非 `rgXX_` 前缀 `catalog_version/account/category/name_history/command_request/command_receipt` 六张产品表，加性纯建结构零回填，旧库由幂等 bootstrap 承接）；v29 按 D-144 增加 P7-02 录入基础新边 28.sqm（非 `rgXX_` 前缀往来对象目录与名称历史、对象级借贷位置与历史、手工转账/借贷 request/receipt 与置顶偏好产品表，加性纯建结构零回填）；v30 按 D-147 增加开卡枚举性能批新边 29.sqm（纯加性单条覆盖索引 `import_duplicate_candidate_subject_idx(ledger_id, subject_source_id)`，零数据回填，沿 26.sqm 纪律由 caller 包外层事务）；v31 按 D-158 增加 P7-05 片 1a 基底层新边 30.sqm（v30→v31，纯加性零回填，`PRAGMA defer_foreign_keys = 1` 由 caller 外层事务包裹，11 个新 DDL 对象：`transaction_void_request`/`transaction_void_fact`/`transaction_void_receipt` 三表、回收站排序索引、四个守卫触发器、`transaction_correction_request`/`transaction_correction_receipt` 两表与视图 `transaction_effective_state`，与 fresh `Ledger.sq` 终态定义逐字节一致）。逐版本 populated migration、child FK preservation、原子拒绝/回滚、fresh schema 与 v1→v31 metadata equality 均有 JVM 测试和 migration verifier。
- `RG-01` create、retry、distinct、7 个拒绝及 `note_update` runtime 已实现。备注替代保留 posting set 与经济事实，并覆盖 replay、request identity conflict、stale CAS 零写入和 operation oracle。完整 state/delta/status 比较已按 `D-087` 实现（`Rg01FullStateOracleTest`，8 roots/11 ops/19 states，merge `f9da4b6`）。
- `RG-02` 完成 `D-071` 批准的 manual-income 最小 slice：create、retry、2 个独立变体与 8 个拒绝；`category_rename` 已按 `D-087` 实现最小闭环（append-only name history，17.sqm）。完整 state/delta/status 比较已实现（`Rg02FullStateOracleTest`，11 roots/13 ops/24 states）。
- `RG-03` 当前冻结范围已实现：13 roots、20 operations 的 outcome、returned IDs、完整 state、deltas 与 status changes 均与 approved expected 精确比较；mapping gate 已 approved，v2 工件已发布（`dec854e`，D-086；13 roots/20 ops/33 states）。
- `RG-04` 的 26 个 raw v1 operations 均有 runtime。全 26 项完整 state/report/reconciliation/delta 比较已合入（`Rg04FullStateOracleTest`，`88c9bfa`，17 roots/26 ops/43 states）。import lifecycle、ownership 与 reconciliation 另有深入持久化测试，approved v2 已发布。
- `RG-05` 的领域、应用与持久化 runtime、schema v8 及相关迁移和测试已经进入共享库。`D-075` 已批准 17 roots、25 operations、42 complete states 的 expected；共享 `GoldenV2Oracle` 与 `Rg05FullStateOracleTest` 对全部 25 operations 比较完整 state、deltas 和 status changes；v2 工件已发布（`dec854e`，D-086；17 roots/25 ops/42 states）。
- `RG-06` 的 Golden Schema v2 契约与语义校验、领域 aggregate、catalog-free validated snapshot rehydration、八 action 应用/原子 commit-port 契约、schema v9 base/v11 migration、SQLDelight adapter、fixture replay 和 publication 已完成。41 个 operation 比较 outcome、returned IDs、完整 state、deltas 与 status changes；发布工件为 `golden/rules-v2/rg-06.json`，manifest 保存 source/expected/canonical/output hashes 与 20/41/61 对象计数。手工付款建立 pending 分录对账；导入候选只在原子精确确认时直接建立 matched 对账；镜像合并不改变对账。
- `RG-07` 的 contract、expected、adapter、fixture replay、schema v10 migration 与 Kotlin runtime 已完成；49 个 operation 比较 outcome、returned IDs、完整 state、deltas 与 status changes，v2 工件已发布到 `golden/rules-v2/rg-07.json`。
- `RG-08` 已有冻结 v1、Python 测试与逐路径 mapping（4969 路径、2964 requires_contract_amendment）；`D-084` 已关闭 4 个 gap（`RG08-GAP-01..04`）并完成完整 lending runtime、44-op oracle 与 schema v15 持久化，mapping gate 已 approved（`rg-08-closure-proposal.md`）；v2 expected 工件（44 ops，accepted 6/no_change 13/rejected 25，18 roots/62 states，SHA-256 `b3434dfc849fffb3c22511577bd0090158a3173d2b1cabfbe6bf9c20d866bca7`）与 publication 已按 `D-089` B 批完成（`golden/rules-v2/rg-08.json`，D-090 LF 验收通过）。`RG-10` 的 13 actions 领域、应用与持久化 runtime、44-op 完整状态 oracle与 schema v13 SQLDelight persistence 已按 `D-083` 批准范围实现并合入 main（`22f3141`），mapping gate 已 approved，closure proposal 已登记，v2 工件已发布（`7cf419a`；44 ops，accepted 12/no_change 10/rejected 22，14 roots/58 states）。RG-09 的 D-082 gaps 已由 closure overlay 标记 closed；50-operation v1 oracle、严格 9-root/50-operation/59-state v2 runtime oracle、D-065 proof、schema v14 migration 与 deterministic publication 均已完成，mapping gate 已 approved。`RG-11`、`RG-12` 为 direct-v2（无 mapping/adapter，expected=冻结契约字节副本，契约批准于 `efbb13a`）；`D-085` 已实现完整 Kotlin runtime、oracle 与 schema v16/v17 持久化，closure gate 已 approved（`rg-11-closure-proposal.md` 与 `rg-12-closure-proposal.md`），v2 工件已发布（`dec854e`；RG-11 22 ops/3 roots/25 states，RG-12 12 ops/3 roots/15 states）。
- golden v2 publication 集合已完整：RG-01 至 RG-12 共 12 case 全部发布（`golden/rules-v2/` 工件 + manifest 登记；RG-01/02 按 `D-089` A 批、RG-08 按 `D-089` B 批，A 批经 `D-090` LF 验收闭合，B 批 gate 与 raw-byte hash 核验通过）。`D-090` publication LF integrity 契约已落地：`.gitattributes` 3 条 EOL 覆盖、共享 pre-publish integrity gate（所有 publisher 无条件接线）、11 case raw-byte hash metadata 重算（canonical 不变）。
- `D-097` 已批准 contract-only P4-01 normalized source、typed diagnostics 与匿名 acceptance，并已登记为表示中立的逻辑 oracle：contract v1 的 complete/partial/rejected、record multiset、完整性与 presence、source/derived rule trace、稳定 diagnostic taxonomy、安全 location、exact decimal/time、九项 synthetic fixtures 和 permutation 规则。P4-01 本身不实现运行时行为；后续实现分别受各批决定约束。
- `D-098` 已批准并交付 P4-02 shared import spine：共享 source/evidence/candidate/confirmation owner、最小 raw identity/retention/provenance、candidate lifecycle 与 atomic confirmation 已进入共享库；schema v21 新增 9 张非 `rgXX_` 的 `import_*` 表，30-operation oracle、迁移、原子失败与重开验证已完成（merge `d756391`）。
- `D-099` 已批准并交付 P4-03 第一标准来源普通收支切片：微信 XLSX parser 采用 Apache POI JVM，fail-closed 解析契约、匿名 synthetic XLSX fixtures 与 P-01～P-21/E-01～E-14 oracle 已通过 shared import spine；该批不创建 matcher/evidence link 或 reconciliation effect（merge `18fae64`）。
- `D-117` 已批准并登记 P5-01 双平台最小外壳契约（contract-only，2026-08-29）：冻结 CMP 栈（CMP `1.11.1` + 主皮库 `0.9.3` 配对，UQ-1 方案 A，主皮库命名见契约 §3.2；`jpackage` 与 Material3 工件延后皮肤批）、桌面驱动（`sqlite-driver:2.3.2` 置 `desktop-app` jvmMain，Android 零改动）、组合根契约（`android-app`/`desktop-app` 两模块，platform-* 偏差延后显式登记，DI/导航维持暂缓）、Clock 端口（`kotlin.time.Instant` 单方法，来源时间不可覆盖）与产品 ID 端口（UUIDv7 惰性物化，实装随 P5-02）；UQ-2 保持 P5-02/P5-03 拆分。零实现、零 schema、零生产行为变更。
- `D-118` 已批准并交付 P5-02 双平台骨架实施批（2026-08-29）：`desktop-app`（KMP jvm-only，CMP `1.11.1` + plugin.compose `2.4.10`，主类 `com.unifiedledger.desktop.MainKt`，jvmMain `sqlite-driver:2.3.2`，空库引导本地测试账本 + `DesktopSkeletonSmokeTest`）与 `android-app`（`com.android.application` 无 kotlin-android，applicationId `com.unifiedledger.android`，minSdk 34/targetSdk 36/compileSdk 36，`activity-compose:1.13.0`，NoActionBar + exported MAIN/LAUNCHER，`AndroidSqliteDriver` 私有库）两组合根；`LedgerClock` 端口（`fun interface now(): Instant`，系统实现置两端组合根）与 UUIDv7 产品 ID（RFC 9562 规范形式，`UuidV7Generator` 纯位打包 + 平台 `SecureRandom` 注入，`UuidV7ConfirmedManualExpenseIdSource` 每次恰 6 个 UUIDv7）；根构建/settings、CI/CONTRIBUTING/README/PROJECT_MAP/ARCHITECTURE 同步；零 schema/迁移变更。
- `D-119` 已批准 P5-01/P5-02 闭环补充契约（2026-08-30）：D-118 保持已交付；冻结 P5-03 的 current-version/ledger-scoped read/options、固定匿名 catalog、精确金额、requestId、snapshot-aware unknown resolution、startup fail-closed 和双端验收边界。该决定只允许按规格顺序起草 P5-03 spec-only 与执行独立 `closure-evidence follow-up`，不授权 P5-03 代码实施；后续 Android、Desktop 和 TalkBack 人工证据已在 P5-03 收口记录中完成，零 schema/生产行为变化。
- `D-120` 已批准并交付 P5-03 演示面 B 实施批（2026-08-30）：新增共享 `app-ui` KMP UI 模块（androidTarget + jvm，compose；Kotlin 2.4.10/CMP 1.11.1/AGP 9.1.0 零升级，`ledger-application`/`ledger-domain` 构建脚本零改动），落地阶段 5 首条可用业务流程（只读总览 + 一条手工支出写路径）；application API 含 `ParseManualExpenseAmount`、`ManualExpenseOptionsProvider`/`QueryManualExpenseOptions`、`LedgerCurrentStateReadPort`/`QueryLedgerCurrentState`、`ResolveManualExpenseCommitStatus`、`ExecuteManualExpenseSubmission` + `CommitOnceInvocationTracker` 与独立 `UuidV7ManualExpenseRequestIdSource`；`SqlDelightLedgerCurrentStateReadAdapter` + `Ledger.sq` 只增 3 条只读查询（零 DDL，schema v27 与 26 个迁移文件不变）；共享 14 状态纯 reducer UI（无 IO/随机/facade）+ 7 异步结果事件，两端组合根 startup fail-closed；Desktop current-schema 重开证据测试；CI 增 `:app-ui:jvmTest` 与 upload-artifact（`android-debug-apk-${{ github.sha }}`，retention 7 天）；CONTRIBUTING/README/PROJECT_MAP/ARCHITECTURE 已同步。实施 commit `8a0a079`、merge `85a4138`，2026-08-30 已推送 `origin/main`；双独立评审 APPROVE + polish + delta closure CONFIRMED，distinct verifier 9/9 PASS，project_docs green，Python 806 tests OK。Android 门期间另推送三个缺陷修复提交（`4ee52fa`/`2cde3a7`/`d616d2e`，均经 CI 验证）；Android、Desktop 键盘/焦点和 TalkBack 人工门均已完成。
- `D-122` 已批准并交付 P5-04.1 三 Tab 与中央新增入口（2026-09-02）：共享 `app-ui` 以 material3 Scaffold + NavigationBar 建立首页/账户/分析三 Tab 壳，新增入口（contentDescription「新增支出」；当时为居中 FAB，布局其后经 `D-123` 调整）复用 `StartNewExpense`；Tab 选择由共享 reducer 承载（`P503Tab`/`SelectTab`，`OverviewEmpty.selectedTab`，权威刷新恒回首页）；账户 Tab 渲染 `LedgerCurrentState.balances` 与 catalog kind 文案；分析 Tab 由 application 层新增纯派生 `SummarizeLedgerActivity` 提供按交易 kind 计数与按币种支出/收入合计（normal-balance 符号规则与 `QueryLedgerCurrentState` 一致，checked 累加/取反 fail-closed，未知账户抛 `IllegalStateException`）；`P503LedgerFacade` 增加第 11 个只读成员，两端组合根同源接线。零 DDL、零新依赖、不引入导航库；实施提交 `ce8e517`、merge `d1bc8f3`（本批未 push，APK artifact 验证随授权 push 后由 CI 承担，R-9；该 APK artifact 承诺已由 D-127/D-128 授权推送后的 CI 工件兑现）；独立验证三模块 jvmTest 394 tests/0 failures、`:android-app:compileDebugKotlin`、三模块 ktlintCheck、project_docs 全部通过；实施规格见 `docs/specs/2026-09-02-p5-04-1-three-tab-shell-implementation-design.md`（冻结 SHA-256 `2ae00ca230aedb9b9b221d227546f7ef442dede2c02b18c0be68f5d0e900acda`）。
- `D-123` 已批准并交付 P5-04.1 底部栏布局增量（2026-09-02）：用户线框裁决新增入口为底部右侧独立圆形按钮（"＋"红色强调、contentDescription「新增支出」），首页/账户/分析 Tab 组居左，两者分离；D-121「中央新增入口」重释为“应用主新增入口”而非几何居中；顶栏（☰/年月）与首页「看板」经用户裁决推迟至后续批次。实施 `1e7955e`/merge `ee3f4fd`；独立验证 `:app-ui:jvmTest` 21 + `:desktop-app:jvmTest` 4（`--rerun-tasks` 全新，0 failures）、ktlintCheck、project_docs 全绿；规格 `docs/specs/2026-09-02-p5-04-1-bottom-bar-layout-delta.md`（冻结 SHA-256 `9a69a2bbf35f3fbd602ccebabb344cbc1d9f78d3c5c9629782e2ffe7a19a496f`）。
- `D-124` 已批准并交付 P5-04.1 底部栏悬浮样式与垂直对齐增量（2026-09-02）：用户指令将 Tab 栏改为脱离屏幕边缘的圆角浮层面板（`NavigationBar` windowInsets 归零、外层 `navigationBarsPadding` 统一承担系统 inset，Tab 组包入圆角 `Surface`），并使「+」按钮中心与 Tab 栏中心处于同一水平线。实施 `f496ea3`/merge `1a7b4c5`；独立验证 app-ui 21 + desktop 4 tests（`--rerun-tasks` 全新 0 failures）、ktlintCheck、project_docs 全绿；规格 `docs/specs/2026-09-02-p5-04-1-floating-delta.md`（冻结 SHA-256 `356ed34128c8d803be8217fa969e08572a66f0b08e6d712d3b43569a5a5aa21d`）。
- `D-143`～`D-146` 已批准并交付阶段 7（Android 日常工作流）第一组（2026-09-15）：P7-01 账户与分类目录管理、P7-02 录入基础、P7-03 看账、P7-04 导入与草稿确认全部实施并分别合入本地 `main`（merge `4eb41ee`/`b56913d`/`b490bea`/`12e3057`），已推送 `origin/main`（push `a710c6c..12e3057`）；head SHA `12e3057` 同提交 CI run `34924060988` 三 job 全绿（Python tests、Kotlin tests、Android compile）并上传 `android-debug-apk-<sha>` 工件；该组剩余开放项为 Android 人工运行证据门（D01：四格式目标 Android 运行证据、义务④ 接治耗时实测量化、SAF 实机观察；该开放项已由 2026-09-15 D01 裁决在披露语义下闭合、权限撤回向量保留 PARTIAL，四格式双平台可用性另由 A-04.1/A-04.2/A-04.3（D-149）于 2026-09-18 以设备证据确立）；该组残余缺口（C01/C04/D06 与 D-145 N3）已由独立残余闭合批于 2026-09-15 在分支 `UL-p7-fix1` 交付闭合（实施登记见 D-146 残余闭合批实施登记）。

## 验证证据

四个 Kotlin 模块的 JVM 测试与 Python 套件在最近一次完整验证中全部通过，零 failure、零 error。逐模块计数随测试增删而变，不在此复制；验证命令见 [README](../README.md)，实际结果以 `build/test-results` 下的报告为准。

- `RG-01`/`RG-02` 完整比较 oracle 验证证据（R3 登记，D-088）：`Rg01FullStateOracleTest`（8 roots / 11 ops / 19 states）与 `Rg02FullStateOracleTest`（11 roots / 13 ops / 24 states）经独立 spec/quality review 双 PASS，distinct verifier 11/11 PASS（D-087，merge `f9da4b6`，2026-08-10 验证记录）；数字以测试文件头注释与 D-087 登记为准。验证记录见 `docs/PROJECT_STATE.local.md`（2026-08-10）。

## 当前环境

- Kotlin Multiplatform 插件版本为 `2.4.20`、Gradle Wrapper 版本为 `9.5.0`、JVM 工具链为 JDK 21；Compose Multiplatform `1.12.0`、Android Gradle Plugin `9.3.1`（D-141 技术栈升级批）。
- 四个 KMP library 的 JVM 测试和根项目 Gradle 检查可在 Windows 上运行；`ledger-data` 的 SQLDelight 迁移验证与 Android target 编译也可独立运行。
- 当前 16 GB Windows 主机上 Gradle/Kotlin 验证必须串行，使用单 worker 和 1 GB heap，并在每次运行后停止 Gradle daemon；具体命令见 `docs/CONTRIBUTING.md`。
- Python 核心测试和文档验证可在 Windows 上运行。
- `ledger-data` 已有 Android 编译目标；共享模块现含新增 `app-ui`（P5-03，androidTarget + jvm，compose）；`desktop-app` 与 `android-app` 两个组合根模块按 P5-02 建立、按 P5-03 只保留平台入口与组合根装配（共享 UI 由 `app-ui` 承载）。桌面应用经 `.\gradlew.bat :desktop-app:run` 启动；Android 调试 APK 经 `.\gradlew.bat :android-app:assembleDebug` 构建（本机 R-9 OOM 时经 CI artifact 获取）；模拟器安装/启动人工门已于 2026-08-30 关闭。
- SQLDelight `2.3.2` 与 Android system SQLite 已确定用于当前正式持久化边界；当前 schema 为 v31。CMP 栈与桌面驱动已确定（D-117），导航、依赖注入、同步与更广泛查询方案仍未选择。

## 当前阶段

阶段 5 为“双端最小外壳与稳定 Android MVP”，已于 2026-09-03 按 `D-130` 完成收口。P5-01、P5-02、P5-03、P5-04.1（三 Tab 与中央新增入口，D-122）、P5-04.2（新增记账全屏编辑页，D-125）、P5-04.3（完善既有手工支出流程，D-126）、P5-04.4（fail-closed 重试策略与失败状态单一来源，D-129）与 P5-04.5（Android 与现有 Desktop 回归验证收口，D-130）已全部交付；阶段 5 收口时（2026-09-03）技术基线为 Kotlin 2.4.10、Compose Multiplatform 1.11.1、Material3 1.9.0、min/compile/target SDK 34/36/36，schema v27；技术栈已于阶段 6 按 D-141 升级（Kotlin 2.4.20 / CMP 1.12.0 / AGP 9.3.1 / targetSdk 37）。主皮库、Backdrop、Liquid Glass 与 SDK 依赖变更按路线图归阶段 6。

- P5-04.1：三 Tab（首页、账户、分析）与新增入口。（已交付，D-122；布局经 D-123 调整为 Tab 左组 + 底右圆形新增按钮，并经 D-124 悬浮胶囊样式与水平对齐）
- P5-04.2：共享状态控制的新增记账全屏编辑页（不引入导航库）；系统返回关闭编辑页，确认/取消行为固定。（已交付，D-125）
- P5-04.3：完善既有手工支出流程，不新增收入、转账或借贷正式类型。（已交付，D-126）
- P5-04.4：启动、加载、提交和失败状态保持 fail-closed；仅 startup error 或 handoff 前可证明 `commitOnce` 零调用的 `InfrastructureFailure` 可恢复重试，`UnknownCommit`、冲突和领域拒绝禁止自动重试。（已交付，D-129，实施 `208e3cf`；CI 新增 `:android-app:testDebugUnitTest` 步骤）
- P5-04.5：Android 与现有 Desktop 回归验证并完成阶段 5 收口。（已收口，D-130，2026-09-03；Android 与 Desktop 回归门全过，阶段 5 完成）

阶段 6 当前状态（2026-09-11）：**已收口**（见 D-142）。

- 阶段 6 入口已过（D-133）；全部批次 D-133（入口证据与 SDK/视觉裁决）、D-134（D2 主题与组件）、D-135（D3 平台回归）、D-136（玻璃启用）、D-137（对话框 Esc 修复）、D-138（发生时间手输宽容解析）、D-139（键入保留缺陷修复）、D-140（冲突/拒绝流键入保留）、D-141（技术栈升级）均已交付并关闭。
- FOUND-001 已按 D-132 闭环，Android 与 Desktop 损坏行为统一；E2E-R-001 状态栏对比度已由 D-134 修复、D-136 复验通过；E2E-R-002（API 34 回归）已关闭；D-131 TalkBack 走查通过。
- 平台与视觉：D-135 完成 Android 14/15/16 模拟器回归与性能采样、targetSdk 37 行为审计七域无影响，API 37 模拟器回归延后项于 2026-09-07 闭合；Material3 稳定基线与玻璃独立回退层均可用。
- 技术栈升级：D-141 交付 CMP `1.12.0` + backdrop `2.0.1` + Kotlin `2.4.20` + AGP `9.3.1` + targetSdk `36→37` + kotlinx-datetime `0.8.0`；双端人工门向量 1-9 全 PASS，CI run `34555111003` 三 job 绿。
- 当前基线：Kotlin `2.4.20` / CMP `1.12.0` / AGP `9.3.1` / Material3 `1.9.0` / min·compile·target SDK `34/37/37`，schema v31。
- 阶段 6 收口完成（D-142）；阶段 7（Android 日常工作流）第一组 P7-01～P7-04 已交付、合入本地 `main` 并推送（D-143/D-144/D-145/D-146；push `a710c6c..12e3057`，head `12e3057` 同提交 CI run `34924060988` 三 job 全绿）；该组后续批次已续交付：D-147 组枚举性能批（v30 覆盖索引 + 会话级批量查询 + 进度面）；A-PERF 导入读治理批（D-148：层0 统计刷新/层1 定向读/层2 主线程 catalog 读治理，merge `e61b9a0`，设备复测 B2/B3 地板校准达标、B6 20k 组开卡 2.61s 门槛 ≤3s；B5 定向详情 ≤1s 门槛**曾未获证实**（校准上界 ~2.4s > 1s，uiautomator dump 地板 ~2.29s/次粗于门槛，故归 A-PERF 后续排查批），该批已按处置要求以设备侧 `/proc/uptime` 括号（单 shell up0→tap→up1，括号 ~70ms）+ `dumpsys gfxinfo framestats` 首帧计时（InputEventId 判别 tap 消费帧）在 20k 库（产品 SAF 路径铺设，统计满足层0）与当前 main 候选 APK 绑定下重测：三轮 0.049/0.049/0.056s（max 0.056s）**决定性 PASS ≤1s**，历史 4.92s/~2.4s 读数确认为 dump 地板伪影；测值为上界（up0 读取与 tap 派发开销 ≈0.3–0.4s 含于测值内），原「未获证实」登记就此解除，CI run 35280369455 三 job 全 success；计划 §2 第 44 行的 B5 测量方法复核要求已按 D-163 履行：既有终点（点击后首个 `IntendedVsync`）不能绑定详情关键内容呈现，新终点绑定冻结规格 §6 对关键内容的唯一括注「（金额/状态行）」——判定为同一无障碍快照内同时存在精确文本 `候选详情`（不取子串，列表行的 `onClickLabel` 为「查看候选详情」）且存在与所点行金额精确相等的文本节点，点击以所点金额节点的可点击祖先派发 `ACTION_CLICK`（不使用合成指针输入），计时为 `SystemClock.uptimeMillis()` 的 25–50 ms 轮询区间上界；在经产品 SAF 路径铺设的 20k 库（2×10k，20,000 候选 / 10,000 重复记录 / 0 交易，`sqlite_stat1` 23 行）上三轮全部有效，区间 `[249,295]` / `[177,217]` / `[158,202]` ms，最大上界 295 ms ≤ 1s，故该复核要求已履行；口径限定两条：该读数为**上界**，与历史首帧读数（**下界**）不是同一命题、不可直接比较；且该库为 0 交易、有效谓词 no-op，故该读数**不构成** D-158 §5 所需的含交易 20k 库有效谓词代价读数（D-163 只补测量方法与证据，不改 D-148 的 B5 PASS，也不改变 §10.3 裁决））；A-04.1 建行 XLS Android instrumented HSSF 验证（D-149：绿裁决，X-4 命题证实，merge `b66bdf3`，CI run 35289045109）；A-04.2+A-04.3 建行 XLS Android 矩阵翻转与产品路径全链设备验收（四格式双平台全可用，merge `c93e18a`，CI run 35301225076）；D01 人工门与义务④已于 2026-09-15 在**披露语义**下闭合（权限撤回向量保留 PARTIAL——AOSP 上无确定性注入点，类型化语义由 JVM 等价链钉死），该披露式闭合不等于四格式可用：四格式双平台可用性由 A-04.1/A-04.2/A-04.3 于 2026-09-18 以设备证据另行确立；P7-05 修错与回收站设计门已按 D-156 批准（规格 `docs/specs/2026-09-19-p7-05-correction-void-recycle-design.md`，approved），其首切片（片 1a 基底层）已按 D-158 交付至 schema v31，片 1b（产品接线）与 P7-06～09 尚未交付。
- 阶段 7 第一组后续批次续交付（2026-09-18～2026-09-19）：`D-150` A-DOC 批正式状态同步与 CCB fixture 隐私清理（7 对 fixture 内嵌 BIFF WriteAccess 账户名替换为合成值 `SYN-CCB-USER`；既有历史中的残余暴露面如实披露，历史重写属独立决定未做）；`D-151` P7-02 A-02 设备验收缺陷修复（置顶状态改由 store 权威结果驱动；确认页按类型渲染并按类型呈现字段与金额构成）；`D-152` P7-02 提交后刷新链缺陷修复（月度小结 AWAITING 的落地时序与编辑流载荷携带、会话内新建往来对象后借出提交的目录权威刷新）；`D-153` P7-03 触发集 (f) 增补（导入批量确认派发运行到达终态触发恰一次权威刷新与月度重请求）；`D-154` P7-04 决策面裁决（导入候选决策面按 face×方向渲染分类源，in 方向 ordinary 候选可选收入分类）。各批均合入本地 `main` 并推送 `origin/main`；同提交 CI run `35334540478`（D-150）/`35383765551`（D-151）/`35400302039`（D-152）/`35419996777`（D-153）/`35436021944`（D-154）三 job 全 success。
- 阶段 7 第一部分 A-05（P7-04 导入全链验收补链）**部分完成，不整体 PASS**（按计划 §10.1 逐项登记；D04-4 补证与 §10.3 规模项收口裁决见 D-157，该规模项的承接复核与遍历阻断成因见 D-159）：**PASS**——D01-1～D01-8/D01-10（四来源产品路径与损坏/DOCTYPE/超限/取消/SAF 向量，设备证据；D01 门证据原绑 APK `10ef6b0`，其后 schema 经 D-147 升至 v30、候选读路径经 D-148 变更，按计划 §10.2 以差异说明 + 轻量重钉三场景（支付宝 L0 超限、招行 oracle、微信损坏）承接候选绑定补链，建行 XLS 由 A-04.2/A-04.3 最新链独立覆盖）、D02-1（确认前正式零写入）、D02-2（incomplete 结构性不可勾选与 typed 文案，设备留证）、D02-3（typed 早拒绝，JVM）、D03-1（同源 replay 零二次影响）、D03-4（合法相似「不是重复」的保留两者语义，设备向量）、D04-1（批量成功呈现）、D04-2（领域拒绝分项恢复，JVM + 设备级补充）、D04-4（批量中段进程中断，D-157 由「缺证/PARTIAL」转 PASS：候选 `2381b6e` 设备注入（与 D-154 设备回归同一 APK 绑定），设备内定时器与确认 tap 同一 shell 注入 force-stop，6 项批在 k=2/6 处被杀；重开核对前缀恰一次——`ledger_transaction` 34→36、`import_confirmation` 34→36（receipts=claims 1:1）、`posting` 68→72（2×2 腿）、`pending_confirmation` 13→11（余 4 项未动），36 个 posting set 全部 2 腿且逐笔平衡、无重复确认、`quick_check` ok）、D04-5（已成功保持一次，JVM）、D05（JVM 维度）、D06-1（规模月度核对与 DB 逐值一致）、D06-2（脱敏）、D06-3（回归保持）；**裁决登记**——D01-9 权限撤回缺证（PARTIAL 保留：计划 §2 A-05 行要求「权限撤回原 PARTIAL 保留，补可控平台测试」并由 A-05 验收裁决登记；理由=AOSP 无确定性注入点、类型化语义由 JVM 等价链钉死、可控平台测试在位；承接=平台或环境提供确定性注入点时按计划 §10.2 重开复测）、D03-2 不适用（导入会话身份为每次选择的随机句柄，改名向量经设计退化）、D03-3 接受延期（JVM 证据接受）、D04-3 接受延期（JVM 接受，设备无确定性 Unknown 注入）、D05 设备维度不适用（生产 ID/重复分支为数据级判定）；开放项见「未完成门槛」。
- 阶段 7 第一部分设备验收批 A-01～A-03 状态（按计划 §10.1 逐项登记）：**A-01（P7-01 账户与分类目录管理）全部向量 PASS**——新增资产/负债账户、两级分类创建与追加、改名保稳定 ID 与名称历史、无引用删除原子性、级联停用与约束拒绝、整组启用、重开持久，均经设备路径 + 权威 DB 核对（另登记 UX 观察 `OBS-A01-SILENT-REJECT`）；**A-02（P7-02 五类型录入与恢复）计划行整体 PASS（设备验收完成）**——写入侧五类型与计划指定借贷向量（借出 100+20 → 收本金 40/利息 5 → 到账 45、本金余 80）、另人隔离、取消零写入、置顶持久与重开持久逐值成立，呈现侧五缺陷（`A02CONFIRM-001`/`A02PIN-001`/`A02PIN-002`/`A02MONTH-001`/`A02LEND-001`）经 D-151/D-152 修复批与设备回归全部 CLOSED；计划点名的**金额算式（B06，P7-02 规格 §7 验收矩阵）**设备向量已执行（候选 `2381b6e`，与 D-154 设备回归同一 APK）：入口页「金额算式（可选：+ - × ÷ 与括号）」+「计算」→ `= <值>` 结果行 +「应用到金额」写入「金额」字段的产品路径逐向量核对——`12.5+8` → `= 20.50`（应用后金额 `20.50`）、`2*(3+4)` → `= 14.00`、`10/4` → `= 2.50` 均 PASS；`1/3`、`1-2` 按 E-3 类型化拒绝（文案分别为「算式结果无法用该币种精度精确表示（如 1/3 或 1/8）」「算式结果为负」，均无结果行与「应用到金额」）PASS；全向量期间正式账零写入（`ledger_transaction` 36 / `import_confirmation` 36 / `posting` 72 不变，`quick_check` ok），入口页未提交关闭。算式算术语义此前已由 JVM 求值器测试族覆盖，本轮设备运行补充的是产品路径（算式字段 → 计算 → 应用 → 拒绝文案）证据；**A-03（P7-03 看账）部分完成**——A03-1～A03-6 与 A03-8a（启动失败态与重试恢复）PASS，A03-7 长列表向量与 A-05 窗口合并后 PASS；TB1～TB7 TalkBack 朗读巡检接受延期与 A03-8b 缺证见「未完成门槛」。

## 未完成门槛

- `D-075` 只批准 RG-05 expected，不授权 adapter 实现或 fixture 迁移；RG-05 的 v2 发布由 `D-086` 另行授权。
- RG-01、RG-02、RG-08 已全部发布（`D-089` A/B 批；A 批经 `D-090` LF 验收闭合，B 批含 LF gate 与 raw-byte hash 核验）；完整比较证据见 `D-087` 与 `D-088` R3 登记。
- RG-05 完整状态 oracle、RG-06 publication 与 RG-07 closure 不表示全部黄金场景或正式账务核心已完成；RG-01、RG-02 的完整 state/delta/status 比较已实现（`D-087`，`f9da4b6`）且 v2 publication 已按 `D-089` A 批完成；RG-08 的 runtime 与 mapping gate 已按 `D-084` 完成（4 个 gap 已关闭），v2 expected 与工件已按 `D-089` B 批发布。
- 其他 RG 的 v1 rewrite、v2 publication 和未完成完整比较仍分别受其现有 gate 约束；RG-03/05/10/11/12 已按 `D-086` 发布，RG-01/02 与 RG-08 已按 `D-089` 发布（`D-090` LF 验收闭合），12 case 集合完整；RG-09 已发布且历史 gaps 已 closed，mapping gate 已 approved，独立高风险 closure review 证据已记录。
- P4-01、P4-02 与 P4-03 已分别由 `D-097`、`D-098` 与 `D-099` 闭环。P4-04 transfer formalization、P4-05 支付宝普通收支与 RL-04 余额宝转账路由已完成；P4-08 matcher 与 reconciliation persistence 已按 `D-103` 合入 `main`（`fd57808`，schema v23）；P4-07 duplicate candidate/closed-records 已按 `D-104`/`D-105` 完成实施并合入 `main`（merge `e1ab7d4`，schema v24），经独立规格增量复审闭环（CLOSURE APPROVE）、全量 `:ledger-data:jvmTest`、Python 全套 806 tests、迁移 verifier、Android 编译与独立 verifier 6/6 验收；P4-06 片 1 信用生命周期（RL-05）已按 `D-107` 实施并合入 `main`（merge `7cf9b79`，schema v25）；片 2（RL-06 混合支付激活）已按 `D-108` 实施并合入、推送 `main`（零 schema 变更，schema 维持 v25；merge `df34388`；`b19a6c1` 已在 `origin/main`），P4-06 批全部完成。P4-09 阶段收口批已实施并合入 `main`（merge `e45626b`，2026-08-23，`--no-ff`，9 文件 +4380/-3：契约/实施规格两文档、失败矩阵节、三收口测试类 2033/1181/588 行、既有测试头注释一行与 D5 文档同步），零 schema、零生产改动，失败矩阵登记于 `docs/GOLDEN_TESTS.md`「P4-09 收口失败矩阵」节，实施登记见 `D-110`；独立 verifier（冻结候选 `36e0611`）验证链第 1-7 层全部通过（三新类聚焦 14/14、迁移 verifier v25 原样通过、三 JVM 模块全量 domain 18 类 111 / application 33 类 256 / data 59 类 393（56 基线 + 3 新类）零失败、Android compileAndroidMain、Python 全套 806/806、project_docs、verify-project full `valid:true`），主代理复跑聚焦证明通过；O-1 必选勾选门已于 2026-08-23 执行通过（用户侧真实微信账单完整收口形态验证，留痕于 `docs/PROJECT_STATE.local.md` 收口验收清单，无真实标识入 tracked 文件），阶段 4 收口完成（判据与证据声明见「唯一下一步」）。第二来源 parser、产品 ID 与 Clock 等其余门仍按适用决定和证据门推进；整文件默认不保存，若提议保存则须先批准生命周期、加密、导出和删除。
- 阶段 4 收口后，O-2/P4-08 精度重标定继续以独立增量完成：第一批 D-111 装配闭合已合入 `main`（`c4466f4`，merge `868bb35`）；第二批 normalized evidence projection 已按 `D-112` 完成——规格经用户批准（UQ-1..UQ-6 按推荐批准），实施 28 文件 +2546/-68（commit `6b515f6`，merge `b8755dd`，schema v25→v26），双独立评审 16 findings 全部回修闭环（处置登记于 D-112「实施登记」段），distinct verifier 五命令全部 exit 0（data 62 套件 30m17s、application、migration verifier、compileAndroidMain、project_docs），pre-push trace `valid:true`（348 commits），并于 2026-08-27 推送 `origin/main`；P5 开启评估前提满足，Phase 5 待用户门禁。
- 阶段 4 收口登记的 correction/successor invalidation 延期已由 D-113 交付解除：规格经用户批准（UQ-1..UQ-8 按推荐），实施 23 文件 +3150/-99（commit `10ebd17`，merge `a3d11bb`，schema v26→v27），双独立评审 14 findings 全部回修闭环（登记于 D-113 实施登记段），distinct verifier 五命令全部 exit 0，trace `valid:true`（353 commits）；D-103:1639 / D-109 O-5 登记的该项延期就此解除，RL-07 银行 parser 门延期由 BP-01（D-116）解除，见下条。
- BP-01 银行 parser 门承接批（D-116）已交付并推送（2026-08-29，实施 merge `0c4a634`/`19a2c95`，push `779d529..0c83f7b`）：CMB 网银 CSV 与 CCB 网银 XLS 解析器（CCB 采用 POI HSSF，为既有依赖传递依赖，证据门继承 D-099）、`BankStatementTransferFlowFormalFactory` 银行侧方向门变体（P4-04 钱包视角冻结 oracle 逐值不变）、余额镜像非阻断 `note` 诊断（`SPINE_BANK_BALANCE_CONTINUITY`/`SPINE_BANK_BALANCE_MISSING`）与 RL-07 镜像代表路径（复用 P4-08 `confirmLink` + D-112 READY 投影；E-11 双形状门：原始 `+08:00` 形状进 `confirmLink` 被 `P408_POSTING_TIME_UNRESOLVED` 类型化拒绝且零写入，镜像适配层 UTC `Z` 规范化后单笔正式转账、bank posting `CHECKED`、零第二笔/零收入）。fixtures 21、解析测试 57、E2E 5 全绿；独立双评审 APPROVE、distinct verifier 19/19 PASS、六命令全量 exit 0、合并后 `verify-project` full `valid:true`；零 schema/迁移变更（schema 维持 v27）、D-103 matcher 组合不变、无 `.external/` 与真实账单值入仓。银行 parser 门（D-109 O-8 / D-099:1540）就此关闭；PDF/其他银行/信用卡未提供样本，属 D-116 边界外待用户。
- P5-03 演示面 B 实施批已交付（D-120，merge `85a4138`，2026-08-30 推送 `origin/main`）；Android APK 工件 + emulator 人工门已关闭（模拟器 `ul_p5_test`，API 36 headless，2026-08-30 真实证据）：CI APK 工件 `android-debug-apk-<sha>` 下载并安装；首次启动渲染空总览（账本：ledger-local-test / 账本为空 / 新增支出），应用私有 `ledger.db` 以 current-schema 创建（user_version=27、215 张表）；force-stop 后同版本重开数据库完好（v27/215 表）且总览重渲染；完整手工支出流程走通（支付账户 asset-payment-local CNY + 类别 expense-category-breakfast，金额 35.80，occurredAt 2026-01-15T00:30:00Z → 继续 → 待确认快照 → 确认提交 → Created → 权威刷新 → 总览显示 EXPENSE 交易 + 两条分录（expense 35.80 / payment -35.80）+ 各账户余额 normal-balance 符号正确）；DB 精确持久化（1 transaction / 1 transaction_version / 2 postings / 1 posting_set / 1 manual_expense_request / 1 confirmed_expense_receipt，零重复；postings = asset-payment-local -3580 minor、expense-account-local +3580 minor）；重启恢复（force-stop + 重启后总览仍显示该交易，正式结果由 DB 恢复）。门期间三个缺陷修复提交经 CI 验证并推送 `origin/main`：`4ee52fa` 组合根启动失败日志化 / `2cde3a7` Android 组合根改接 `AndroidLedgerDatabaseHandle` / `d616d2e` 移除 `ledger-data` androidMain `ForeignKeysCallback.onConfigure` 的 `PRAGMA busy_timeout`（桌面 JDBC 保留）。Desktop 键盘/焦点人工门已通过：用户全程不使用鼠标，使用 `Tab`、`Enter`、空格键完成新增支出流程并出现 Created；Android TalkBack 人工门已通过：用户使用 CI 固定 APK 完成 TalkBack 流程，控件可聚焦/激活、输入可用、提交结果可感知。P5-03 双端人工流程门全部完成；R-9 本地 `:android-app:assembleDebug` OOM（APK 经 CI artifact）；R-17 本地聚合 Gradle `check` 在 1 GB Kotlin daemon 上限下 OOM（聚合 check 归 CI）。
- D-121 已批准登记阶段映射与 P5-04 基础交互规划；P5-04 各子批已按 D-122/D-125/D-126/D-129 交付，阶段 5 已按 D-130 收口。
- 残余项保持可见、不得合并签收（计划 §10.4.5）：D01 权限撤回向量为 PARTIAL（原文「归 D01 族后续设备窗口」按计划 §2 A-05 行「权限撤回原 PARTIAL 保留，补可控平台测试」修订——AOSP 无确定性注入点、类型化语义由 JVM 等价链钉死、可控平台测试在位，分母保留、不记 PASS；承接=平台或环境提供确定性注入点时按计划 §10.2 重开复测）；A-PERF 范围外观察项 OBS-APERF-INPUT-FREEZE（合成长连发 fling 后列表指针输入冻结，真实用户手势能否触发未验证）与 50k 冷读 31.62s（超出冻结 20k 口径）留独立裁决（归 A-PERF 后续排查批）；A-PERF B5 定向详情 ≤1s 门槛曾未获证实（校准上界 ~2.4s > 1s，uiautomator dump 地板 ~2.29s/次粗于门槛），已由 A-PERF 后续排查批以设备侧 `/proc/uptime` 括号 + `dumpsys gfxinfo framestats` 首帧计时在 20k 库、当前 main 候选 APK 上重测解除（三轮 0.049/0.049/0.056s，max 0.056s 决定性 PASS ≤1s；测值为上界，含 up0 读取与 tap 派发开销 ≈0.3–0.4s；历史 4.92s/~2.4s 为 dump 地板伪影）；计划 §2 第 44 行的 B5 测量方法复核要求已按 D-163 履行——新终点绑定规格 §6 的「（金额/状态行）」定义（同一无障碍快照内精确文本 `候选详情` 且存在与所点行金额精确相等的文本节点），在经产品 SAF 路径铺设的 20k 库（2×10k，20,000 候选 / 10,000 重复记录 / 0 交易，`sqlite_stat1` 23 行）上三轮全部有效，区间 `[249,295]`/`[177,217]`/`[158,202]` ms，最大上界 295 ms ≤ 1s；该读数为上界、与历史首帧（下界）读数不可直接比较，且因该库 0 交易而不构成 D-158 §5 的有效谓词代价读数。
- A-05 开放项（承接明确，不得合并签收；按 D-157 收口裁决登记）：D04-4 批量中段进程中断已由 D-157 由「缺证/PARTIAL」转 PASS（候选 `2381b6e` 设备注入在 k=2/6 处杀进程，重开核对为无半提交——逐项原子：前缀 k=2 已入账、余 4 项未动，receipts=claims 1:1、`quick_check` ok），不再列为开放项；计划 §10.3 的 10k 级流水规模项按 D-157 裁决为**缺证（部分证据在位）+ 承接**——在位部分证据=D01 门 10k 接治实测（绑 APK `10ef6b0`，按 §10.2 以差异说明承接，见「当前阶段」）、A-PERF 的 20k 级库与修复后 B2/B3/B5/B6 测量（B6 的 20,000 条组冷开 2.61s 位于 ≤3s 门槛内，测于累积至 30,039 候选 / 30,010 重复候选的库，非经产品 SAF 路径重铺的 20,000 候选 / 10,000 重复候选库；B5 定向详情门槛在经产品 SAF 路径重铺的 20k 库上重测决定性 PASS ≤1s）、设备流水列表向量（A03-7）在 12 行流水规模 PASS（当前设备账本累计 36 笔交易，该列表向量未随规模重跑）、D-159 在 50k 库（步骤之间不清库）上的就地四阶段证据（冷启动→首页可操作、进入导入→列表可操作、候选清单可见、候选详情打开 Δ 2.924s；绑 APK `70a8c71` 并按 §10.2 以差异说明承接——该库为 0 交易 0 作废事实，有效谓词 no-op，故 v30→v31 读路径变更对该库上测量的阶段不具实质影响，该结论不适用于含交易库上测量的阶段；该证据覆盖「多次累积 30k 候选」条件，未覆盖「单次 10k 导入」「20k 重复组」数据集与「保留无重复与部分已处置状态」两项状态条件）；缺证=所需规模的单一连续会话覆盖整链全流程端到端、30k 累积候选状态（上述 30k 库读数只覆盖 B6 单向量且该累积库状态未保留）、已丢失的早期 50k 库，以及计划 :190 的逐阶段核对与 :192 的 P7-05～09 后累积数据复验在所需规模上的证据；承接=A-PERF 后续排查批（经产品路径重铺 20k/50k 库并执行受影响向量），:192 的复验按计划在 P7-05～09 各批验收时履行，该证据出现前不记该规模项 PASS；D-159 复核实测定位剩余四阶段未达的成因=机械 fling 遍历约 50 次后触发 `OBS-APERF-INPUT-FREEZE`（可见行钉住、慢速滑动不移动、BACK 仍响应、CPU 0.0%、`PendingEvent: <none>`、force-stop 恢复），**阻断的正是 §10.3 的遍历本身**，且替代输入通道（键盘逐页推进、`MOVE_END`/`DPAD` 无有效推进、轨迹球不滚动）在该规模不可用（该三条读数未入证据工件，仅定性登记，不作定量断言）；D-159 同期另登记三项：(f)/FIX-MONTH-1 的 20k 计时向量（最大值 tap→commit 9.970 s、tap→lastframe 12.474 s；三轮非同质，r1 明显快于 r2/r3）、INPUT-FREEZE 阈值实验（20k 人速 5/5 不触发、机械风暴第 6 次触发，50k 第 2 次触发——阈值随规模显著提前，为 `OBS-APERF-INPUT-FREEZE` 的归属依据）与 50k 冷读分解（未复现 r7 记录的 31.62 s 量级，属基线参照而非同质对照）；承接据此细化为先登记并实现大规模库遍历的**程序化滚动**（Compose semantics 滚动动作或 instrumented 驱动，属测试/自动化基础设施、须先行登记再实施），再补齐「重复组打开→审核/确认→详情与月度刷新→杀进程后重开」四阶段，然后按 §10.1/§10.4 重评——规模项维持非 PASS、无残余并入 PASS；该基础设施已按 D-161 登记并交付：`android-app/src/androidTest` 下的 instrumented 无障碍滚动致动器（沿用既有 instrumentation source set 与 `androidx.test:runner`，零新依赖；遍历与滚动全程不使用合成指针事件——`input swipe`/`input tap` 正是 INPUT-FREEZE 的触发模式；`ACTION_SCROLL_TO_POSITION` 经设备实测不被该列表提供，故 `ACTION_SCROLL_FORWARD` 回退路径即真实路径；CI 维持零 `connectedAndroidTest`，该致动器仅在本地受管 AVD 上作为验收证据工具运行、其读数不构成产品能力声明）；D-162 已在该致动器可用后于 50,600 候选库上取得剩余四阶段的**直接设备证据**（同一库、步骤之间从未清库；库状态 50,600 候选 / 50,600 来源 / 103,600 重复记录 / 1 交易 / 1 确认 / 2 posting，重复状态 DEFERRED 102,200 + CONFIRMED_DUPLICATE 1,400，即「部分已处置」与「无重复」行均在位，`user_version=31`、`quick_check` ok）：(i) 重复组打开 PASS——致动器以 `ACTION_SCROLL_FORWARD` 遍历 4,575 次动作 / 1,052,039 ms（≈17.5 min）后 `整组标记为重复` 进入可见窗口并被点击，**全程未触发 INPUT-FREEZE**（D-159 记录的机械 fling 冻结在无障碍动作通道不复现）；(ii) 审核/确认 PASS——整组处置产生**恰 1,400 条**重复记录效果（`import_duplicate_status_history` +1,400，DEFERRED 103,600→102,200、CONFIRMED_DUPLICATE 0→1,400），而 `import_candidate`/`import_source_record`/`import_duplicate_candidate`/`ledger_transaction` 增量全为 0（组处置不产生资金效果）；(iii) 详情与月度刷新 PASS——详情屏完整渲染、补齐决策后 `进入批量确认`，授权后 `ledger_transaction` 0→1、`import_confirmation` 0→1、`posting`=2（两腿平衡），HOME 月度小结切至 2026-08 显示「交易 1 笔」「CNY：普通收入 0.00，净支出 1.00，结余 -1.00」；(iv) 杀进程后重开 PASS——`am force-stop` 后重开 schema v31 与上述全部计数保持、`quick_check` ok、应用正常渲染；应用侧 ANR/FATAL/OOM 记录 0 次（logcat 与 `dumpsys dropbox` 双查）。**D-162 同时登记四项限定（不并入 PASS）**：(a) 阶段顺序未在单一连续会话内满足——四阶段证据分属两个应用进程周期（instrumented 会话覆盖至「重复组打开→组确认」，该会话结束即充当计划链条末段的「杀进程」，候选详情、批量确认与月度刷新是在重开后的新会话中测得），而 §10.3 链条顺序为「…详情与月度刷新→杀进程后重开」，故链条顺序一致性仍是缺口；(b) 数据条件「单次 10k 导入」未在本证据集内实测（本轮会话内导入为 200 行，50,000 候选基数来自此前会话的五轮 10k 导入）；(c) 数据条件「20k 重复组」未覆盖但**可构造**——单文件接治上限 10,000 **候选**，而组内条目按**重复记录**计（该会话 200 候选→1,400 条 = 7×），D-157 第 4 条亦已登记过 20,000 条组，故属未构造而非结构性不可能；(d)「完整计数」向量未获证实——遍历起点读到的 `collectionInfo rowCount` 与同期库计数不一致且三次运行呈系统性偏移（库 50,000→50,011、50,200→50,211、50,400→50,411，均为入库前候选数 + 11），「列表未随会话内导入刷新」与「`collectionInfo` 计数为陈旧快照」两种读法现有证据不足以区分，故该项登记为开放观察、须另行取证（不据此判定产品缺陷）；另 D-162 登记两项同批限定（正式状态不重复展开，见 D-162 第 4 条）：本轮无拒绝/零写入向量被触发，且组处置的逐项结果仅以 DB 直方图为准（致动器自身的卡内文案统计只覆盖可见窗口，不能作为完整分母）；致动器自身两处残余（组卡打开判定粒度、遍历重取容器时的句柄保留）与运行期 SystemUI 弹窗属测试基础设施与环境事项。**增量证据（D-163 第 4(g) 条）**：D-163 的 20k 库测量在同一会话**无任何导入**的情况下仍报 `collectionInfo rowCount=20011`（= 20,000 + 11），与上述偏移一致，倾向于「常量容器项偏移」读法（**该倾向已由 D-164 第 4(c) 条收窄修正**），开放观察据此收窄但维持开放；**D-162 的增量是 D-159 登记的四阶段缺证已由直接设备证据取代、其成因（INPUT-FREEZE 阻断遍历）已由程序化滚动使能项解除，§10.3 规模项仍维持「缺证（部分证据在位）+ 承接」、不记 PASS**（D-157/D-159/D-162）；**增量证据（D-164 单次会话全链）**：§10.3 链条已**在一次连续 instrumented 会话内**执行（冷启动→首页→进入导入→接治→收尾重读→列表可操作→候选详情→滚动→重复组打开→审核/确认→详情与月度刷新；其中**候选详情与滚动按低成本顺序互换**，属对计划箭头顺序的**已登记偏差**，其余相邻阶段顺序一致），会话结束时由 instrumentation 结束触发的进程终止充当链条末段的「杀进程」、主机侧随即 `am start` 完成「重开」，**D-162 第 4(a) 条（单一连续会话的链条连续性）缺口据此闭合**；该会话在同一 v31 库（由备份回灌恢复的 50,600 候选 / 1 交易，步骤之间从未清库）上完成，会话内由主机侧执行一次产品 SAF 导入（200 行），逐阶段权威读数为：接治 `import_candidate` 50,600→50,800（**+200**）、重复状态历史 **+1,600**；候选详情屏（点首行→补齐决策→勾选→返回）**零经济效果**（决策补全与勾选均无账务写入）；遍历起点 `collectionInfo rowCount=50614`，**4,600 次 `ACTION_SCROLL_FORWARD` / 1,052,923 ms** 后 `整组标记为重复` 进入可见窗口并被点击、**全程不触发 INPUT-FREEZE**；组卡页脚 `确认整组标记` 需再 **389 次动作 / 146,164 ms**、整组处置 **76,687 ms**，产生重复状态历史 **+1,600**（`CONFIRMED_DUPLICATE` +1,600）而候选/来源/重复候选/交易**增量全 0**；批量确认经权威回读产生 `ledger_transaction` 1→2、`import_confirmation` 1→2、`posting` 2→4（**39,426 ms**）；会话结束进程终止后 `am start -W` TotalTime **1,587 ms**，`CONFIRMED_DUPLICATE=3,000`、`DEFERRED=102,200`、50,800 候选 / 2 交易 / 2 确认 / 4 posting、v31 与 `quick_check` ok **全部保持**。**D-164 同时登记剩余四项限定（不并入 PASS）**：(a) 数据条件「单次 10k 导入」**仍未实测**（本会话内导入为 200 行）；(b) 数据条件「20k 重复组」**仍未构造**，并登记一项**结构性成本观察**——组卡条目**内联渲染在列表末尾的批量入口之前**，组越大则批量入口越远（按卡页脚实测速率 1,600 条 ÷ 389 次动作 **≈4.1 项/动作**、**≈0.376 s/动作**推算，20,000 条组的卡页脚需约 **4,900 次动作 ≈31 min**、70,000 条组需约 17,000 次动作 ≈1.8 h；且 `cardScrollActions` 默认 4,000 **低于该推算所需**，复跑须显式调高），故该条件的构造应明确规模口径或改用**关闭组卡**的流程；(c) 「完整计数」观察**维持开放并据本条修正**——本会话遍历起点 `rowCount=50,614` 相对**入库前** 50,600 候选为 **+14** 偏移，与 D-162 的 **+11** **不一致**，故「常量偏移」读法**不成立**，可确定的表述为 **`rowCount` 小于接治后的库计数**（该 200 个会话内候选**未被反映在 `rowCount` 中**）、而相对入库前的偏移由 +11 变为 +14 且**该差异本身未有解释**，两种读法（列表未随会话内导入刷新 vs `collectionInfo` 为陈旧快照）**仍未区分**、须专门取证，且 **D-163 第 4(g) 条的「常量容器项偏移」倾向据此收窄修正**；(d) 本轮**无拒绝/零写入向量被触发**，应用侧 ANR/FATAL/OOM 记录 **0 次**；**§10.3 规模项仍维持「缺证（部分证据在位）+ 承接」、不记 PASS**（D-157/D-159/D-162/D-164）。**增量证据（D-165 数据条件）**：在 D-164 结束状态的库上执行**一次**产品 SAF 路径 10k 导入——应用自报权威结果 `接治完成：新增 10000，等价重放 0，解析拒绝 0，接治拒绝 0。`，DB 增量 `import_candidate` 50,800→**60,800**（+10,000）、`import_duplicate_candidate` 105,200→**156,000**（+50,800，即 10,000 候选对既有来源产生 5.08× 重复关系），故**「单次 10k 导入」数据条件覆盖**；该会话的重复组规模为 **50,800 条**（≥20k），故**「20k 重复组」数据条件覆盖**（口径说明：`import_duplicate_candidate` 每行即一条重复关系，其 `candidate_id` 与 `import_candidate.candidate_id` 属不同命名空间）。**D-165 同时登记三项限定**：① 该组规模上的**卡页脚/批量入口可达性未实测**（50,800 条组按实测速率推算需 ≈12,400 次动作 ≈1.3 h，超出致动器默认边界），承接须明确该条件读法或改用关闭组卡流程（该可达性约束同时决定同规模链条「重复组打开→审核/确认→批量确认」是否可行，不能只读作界面可达性问题）；② **接治耗时观察**（非门）：主机侧选择与派发 34 s、接治稳定耗时按留存工件可界定口径 **≈11.2 min**（新批次 `generated_at` → 结果 dump 落盘；30 s 粒度轮询观察，该轮询日志未入工件），同库上 200 行导入约 111 s 稳定（有工件），耗时随库规模增长；③ 「完整计数」观察**仍未闭合**（未做专门取证）。**§10.3 规模项仍维持「缺证（部分证据在位）+ 承接」、不记 PASS**（D-157/D-159/D-162/D-164/D-165）；`A05BATCHITEM-STALE-SELECTION-001`（低，OPEN——批量确认成功后已终结候选仍计入下一批选择集，确认时逐项跳过且零写入，记账正确性与 exactly-once 未受影响；本轮再次实证（观测计数与列表实际勾选不一致），本次注入批次计数正确（6 项））。**增量证据（D-166 postScroll 决定性取证与 OOM 缺陷登记）**：countProbe 仪器（androidTest `ImportScaleTraversalInstrumentedTest.kt`，随 `3795825` 合入 main）在 61,000 候选库（schema v31）上取得**决定性读数**——baseline `rowCount=61013`（DB 61,000）→ 会话内产品 SAF 导入 +200（DB 61,200，稳定 3 次）→ `postImport rowCount=61013` → 点「刷新清单」→ `postRefresh rowCount=61013` → **60 次 `ACTION_SCROLL_FORWARD` 强制布局后 `postScroll rowCount=61013`**（证据 `local/artifacts/p7-05-scale/ul-scale-traversal-device-20260921-after-postscroll.log` 第 892–893 行；instr `instr-countprobe-postscroll.log` OK (1 test)）——**强制布局后读数仍不动 ⇒ 排除 B（陈旧 CollectionInfo）、A（渲染列表不含会话内导入的新候选）成立，「完整计数」A/B 判定闭合**；产品自渲染组头计数复核交叉印证（导入 unique20（+20）后 DB 第一组「待确认——缺用户决策」应计 10,018，显式「刷新清单」后渲染组头仍为（9998），渲染（`view.rows` 投影）不含会话内导入）；**61k 规模 OOM 缺陷登记**——冷启动/刷新触发 `loadImportReviewRows`（`importReviewRowsForLedger` executeAsList 整表物化）在 61k+ 库上 OutOfMemoryError 两次（设备时 10:30:27、10:57:40；栈 `SqlDelightImportReviewReadAdapter.kt:46` → `LedgerQueries.kt:6604`；堆 192 MB 上限、进程死亡），**§10.3「完整计数」在 61k 规模当前不可达（加载即崩）**，D-148 读治理未覆盖整表物化内存、A-PERF 门槛在 ≤30k 尺度测得（承接=缺陷修复或规模口径裁决）；**countProbe 标签反转修复已合并**——原候选 `10b1c1e` 的 `readingAfterScroll` 标签语义与 step 4b 注释相反（评审阻断项 SPEC-01），已随 `a022c59`+`6830971` 修复为纯观察字段（`postScrollCountMoved`/`postScrollRowDelta`/`postScrollMoveGate`），评审闭合 ACCEPT + distinct verifier ACCEPT，合并 `3795825`；**§10.3 规模项仍维持「缺证（部分证据在位）+ 承接」、不记 PASS**（D-157/D-159/D-162/D-163/D-164/D-165/D-166），剩余缺口收窄为 61k 规模完整计数受 OOM 阻断（承接=缺陷修复或规模口径裁决）与 ≥20k 组卡页脚/批量入口可达性（承接=D-165 第 4 条）。**增量证据（D-169 OOM 修复设备复验与 61k 可操作性缺陷登记）**：D-168 批准的 OOM 修复已实施、合并 `1398e72`（同提交 CI run `35746825277` 三 job 全 success）并经设备复验——AVD `ul_p7_d01`、隔离 adb 端口 5038、API 36、系统 SQLite 3.44.3、APK sha256 `63b2897f629bce769b0082f82be4045e9c051b7b0dbcf6ead374e4ebd27d1b8d`，在 61,220 候选 / 160,200 重复关系 / 2 交易 / schema v31 库上冷启动、「刷新清单」与重复冷启动（≥8 轮）**零 OOM/FATAL/ANR**、进程存活且列表渲染，峰值堆合计远低于规格冻结 ≤160 MB（保留的峰值堆采样 `peak-heap-poll-raw.txt` 本运行最大 Java 81,668 KB + Native 19,636 KB，为写者单次运行值、存在跨运行波动，先前报告 98,372/19,912 KB 属另一运行；修复前基线为 D-166 的 192 MB 堆下两次 OOM），countProbe 在 61k 规模**首次可达**（oomfix 证据日志 `instr-countprobe-postscroll-oomfix.log` 1,000 行、以 `countProbe: end` 收尾、零 fault；`postScroll rowCount=61637`、`dbCandidates=61420`、`reading=indeterminate`——**完整计数观察仍维持开放、未闭合**）；同时**新登记 61k 规模可操作性开放缺陷**——冷首次进入列表 tap→内容可见 6 轮 10869/11000/11459/11397/11695/11705 ms（≈11.0–11.7 s，**截图轮询口径**、非冻结 B2）、刷新重读 ≈9–11 s，**超出规格冻结 ≤3 s**，证据将成本定位在 UI 层（导入屏在组合线程上急切构建整个渲染模型且无 `remember`，`app-ui/.../P503ImportReviewPresentation.kt` 的 `importReviewRenderItems`/`importCandidateClassGroups`）而**非**分页读（20k 对照库 3 轮 2621/3159/2613 ms 为同一截图轮询口径、含 ~2.4 s 地板故**不**作达标依据，达标依据为同 APK 冻结 B2 首帧 0.035/0.035/0.041 s；设备原始分页 SQL 首批 35 ms、完整 20 列投影读 290 ms、查询计划走既有主键覆盖索引（EXPLAIN 工件仅证查询计划；零 DDL 依据为 D-168 已合并的 `.sq` 实现仅新增只读 SELECT、无 schema 变更）），须先仪器化归因再处置；`ACC-D-CONN-01`（连接持有，未仪器化）与 `ACC-D-20K-01`（完整 20k B2/B3/B6 回归）仍开放、不得豁免；**D-166 的 OOM 缺陷本身已修复并经设备复验，但修复自身冻结阈值仅部分裁决（≤160 MB 与零 ANR/OOM 通过、≤3 s 在 61k 未达）；§10.3 规模项仍维持「缺证（部分证据在位）+ 承接」、不记 PASS**（D-157/D-159/D-162～D-169）。**增量读数（D-170 残余设备向量登记）**：D-169 第 6 条留作开放的两条设备向量现已**覆盖**（`ACC-D-CONN-01` 记录「未观察到 ANR」；`ACC-D-20K-01` **仅 B2/B3 部分覆盖**、B6 组卡打开未测量维持开放；附单次运行限定；同 APK sha256 `63b2897f629bce769b0082f82be4045e9c051b7b0dbcf6ead374e4ebd27d1b8d`、AVD `ul_p7_d01`、隔离 adb 5038）——`ACC-D-CONN-01`（连接持有 / D-148 ANR 链）：冷启动→点导入（单条非 WAL 连接上单个 `BEGIN IMMEDIATE` 内 7 批分页读）→2 s 后点 HOME Tab，HOME 正常渲染、logcat 扫描 `ANR in`/`Input dispatching timed out`/`Application Not Responding` **零匹配**、零 `ANR in com.unifiedledger`，即更长持有窗口**未触发** D-148 ANR 链形态（**单次运行观察**、持有窗口**未仪器化**，仅记「未观察到 ANR」而非实测持有时长；工件 `acc-d-conn-01-reading.txt`）；`ACC-D-20K-01`（20k 回归，库 `ul20k-measure.db`：20,000 候选 / 10,000 重复关系 / `sqlite_stat1` 在位，**schema 未经验证**——保留宿主工件为 `user_version=30`、应用打开时可能迁移至 v31 但无证据记录，截图轮询口径，**仅覆盖 B2/B3**、B6 组卡打开未测量维持开放）：B2 冷进入 **2708/2608/3204 ms**（3 轮）、B3 热刷新 **1303/1459/1351 ms**（3 轮）、故障 **0**，对比 D-148 **修复后（OOM 修复前）**原始读数——B2 **4.93 s**（10,039 候选）、B3 **5.03 s**（20,039 候选），地板校准 ≤ ~2.7 s，D-148 的**修复前**基线为同场景 **300+s 卡死**——**无回归**、位于历史原始带之内或之下（该对比为**回归核对**，非 A-PERF 门槛重新冻结——门槛为冻结 B2 首帧方法、20k 冷进入对照 0.035/0.035/0.041 s；本条 B2/B3 三轮与 D-169 登记的 20k 冷渲染 2621/3159/2613 ms 属同一向量的两次独立单次运行、分歧属预期不构成矛盾；工件 `acc-d-20k-01-reading.txt`）；两条读数均为**写者单次运行值**、**不声称跨运行可复现性**；OOM 修复其余开放项**不变**——**61k 规模可操作性缺陷（D-169）维持开放**，冻结阈值仍**只获部分裁决**（≤160 MB 与零 ANR/OOM 通过、≤3 s 在 61k 未达），**凡未获证据者不记 PASS**；本条**零产品代码改动**。**增量读数（D-171 宿主侧归因登记与 D-169 定位更正）**：D-169 第 5 条登记的 61k 可操作性缺陷（≈11 s）经两条 test-only 归因测试在**宿主侧**分解——side A（分页读 + 折叠，`ledger-data/src/jvmTest/.../ImportReview61kReadAttributionTest.kt`，随 `bab7080` 合入）在真实 61k 夹具副本（经环境变量 `UL_D169_FIXTURE_DB` 指向）上 ≈ **2.0–2.4 s/轮**（`D169ATTR: side=A`）；side B（急切渲染模型构建，`app-ui/src/commonTest/.../P503ImportReviewRenderModelAttributionTest.kt`）在 61,000 行列表（渲染模型 61,011 条）上 ≈ **10–50 ms/轮**（`D169ATTR: side=B`）；两者合计 ≈ **2.4 s**，残余 ≈ **8.6 s 既不能由分页读解释、也不能由渲染模型构建解释**（side B 相对 ≈11 s 可忽略），**最可能**是设备侧约 61k 条目列表的 UI 组合/重组（或 GC）成本——两条宿主侧测试均不测量设备侧，故**无已证明的设备侧根因**、**下一步是设备侧测量**（主线程帧/组合墙钟），**不**据此进入修复批；**D-169 的代码定位（「成本在 UI 层：组合线程上急切构建整个渲染模型且无 `remember`」）被 side B 的 10–50 ms 证伪**，但 D-169 登记的缺陷、其开放状态与其「须仪器化/无已证明根因」的既有对冲**均不变**（D-169 未主张已证明根因），本条**更正的是推理、不是缺陷登记**；同批更正了 side B 测试文件一处类注释（原称合成 61,000 行构成「单一」组，与自身「TWO group headers」注释及代码相矛盾——实为 10,000 待确认 + 51,000 疑似重复**两个**非空组；prose-only，不动任何断言）；两测试均为 test-only、**零产品代码改动**；**61k 可操作性缺陷（D-169）维持开放**、冻结阈值仍**只获部分裁决**（≤160 MB 与零 ANR/OOM 通过、≤3 s 在 61k 未达）；两条读数均为**宿主 JVM 单次运行值**、**不具跨运行可复现性**、**无留存原始工件**（测试可重跑），仅作区间引用、不取点估计；**§10.3 规模项仍维持「缺证（部分证据在位）+ 承接」、不记 PASS**（D-157/D-159/D-162～D-171）。**增量读数（D-172 设备侧 D-158 §5 有效谓词代价）**：D-167 第 4(a) 条留作 P7-05 片 1b **开放前置条件**的**设备侧** D-158 §5 读数已取得——AVD `ul_p7_d01`、隔离 adb 5038、API 36、系统 SQLite 3.44.3（**设备引擎**，非宿主 JDBC），库 20,002 交易 / 40,004 posting（干净 v30 经应用打开迁移至 v31，`ANALYZE` 先行、生产 intake 完成语义），方法为设备侧 `sqlite3 .timer on` 围绕两生产查询精确 SELECT 形状的带谓词 join 与消融各 3 轮（以 delta 为信号，两侧同付进程启动成本）：`currentVersionRowsForLedger` 带 **0.061/0.059/0.059 s**、消融 **0.041/0.041/0.042 s**、delta **+20/+18/+17 ms（~+44%）**；`ledgerEntryRowsForLedger` 带 **0.060/0.060/0.064 s**、消融 **0.041/0.041/0.042 s**、delta **+19/+19/+22 ms（~+46%）**——即约 **+17–22 ms（相对约 +44–46%）**、消融侧约 41 ms；结论为该有效谓词在 20k 交易 / 40k posting 设备规模上只增一项**小且一致的绝对成本**（约 +18–23 ms），**远低于**用户可感知门限、**不是** D-158 §5 处置条件（索引边或另立决定）所针对的数量级回归，故 **D-158 §5 的设备前置条件已满足、其处置条件未被触发**（与宿主侧 D-167 读数约 +12–21 ms 方向一致，但本条为 D-167 明确未提供的**设备**读数）；限定：绝对值为上界（含进程启动）、单设备会话 3 轮、库为同质（单版本零作废事实）故谓词**过滤**成本未被行使、本读数不测量该路径；读数为**写者单次运行值**、**不声称跨运行可复现性**（原始工件 `local/artifacts/p7-05-scale/d158s5-device-reading.txt` 为留存证据，gitignored）；本条满足 D-167 §4(a) / D-158 §5 的设备前置条件、P7-05 片 1b **现可推进**，但片 1b **尚未开始**、仍待实施，除本读数外**无任何项记为 PASS**，D-167 的宿主侧读数与其限定**不变**；本条**零产品代码改动**。
- A-03 未竟项（单列披露，不得合并签收）：TB1～TB7 TalkBack 朗读巡检**接受延期**（用户 2026-09-19 明确裁决；理由=Accessibility Suite 在 headless AVD 的 ANR 环境限制、规模巡检需真机/GUI 人工窗口；影响=A-03 该子项不记 PASS、分母保留；承接=捕获协议与 Alt+方向键遍历方法已固化，未来真机/GUI 窗口可直接执行，已捕获的 TB2/TB6 部分朗读证据保留有效）；A03-8b 会话内瞬态读失败→重试**缺证**（WAL+页缓存+单连接使运行中连接对文件锁/损坏注入不可见，按计划 §10.2 登记为不可观察，需真实环境故障）。

## 唯一下一步

**唯一下一步 = A-05 收口项与 P7-05～09 按批裁决推进**：阶段 7 第一组 P7-01～P7-04 及其后续性能/验证/缺陷修复批（D-147～D-154）已全部交付、合入本地 `main` 并推送 `origin/main`（`840bf67`（D-160 A-DOC 批：schema v31 正式状态同步）、`d9fb57e`（D-161/D-162：程序化滚动致动器与 §10.3 四阶段规模证据）、`3cdf555`（D-163：B5 测量方法复核）、`b9a2a2c`（D-164：§10.3 单次会话全链实测）、`6ed4797`（D-165：§10.3 数据条件实测）与 `3795825`/`83f92bf`（D-166：postScroll 决定性取证与 61k 规模 OOM 缺陷登记）均已合入并推送；注：`b9a2a2c`（D-164）的推送 run `35549590604` 被 CI concurrency 取消，其树由后继 `6ed4797` 的 success run `35551340309` 涵盖；D-148～D-154 各批同提交 CI 三 job success 见「当前阶段」；其后两项**工件**合并 `ce8d995`（D-158 §5 有效谓词代价读数）与 `9979f96`（P7-04 导入审核列表 OOM 修复设计批准）已合入本地 `main`；**D-167/D-168 两条决定条目**已随登记提交合入并推送，其后的 D-168 OOM 修复实施批已合并 `1398e72`（`86b3b86` 分页读实施 / `bbdafbf` 等价性测试 / `28a9599` ktlint 修复）并推送 `origin/main`，该 head 同提交 CI run `35746825277` 三 job 全 success；**D-169 一条决定条目**已随后续登记合并 `50e49eb` 推送 `origin/main`，该 head 同提交 CI run `35767226297` 三 job（Kotlin tests / Android compile / Python tests）全 success；其后 **D-170 一条决定条目**已随登记合并 `bdfe036` 推送 `origin/main`（同提交 CI run `35789460236` 三 job 全 success），D-169 的宿主侧归因测试（side A/side B）则随合并 `cfab272` 推送（该 head 同提交 CI run `35798905206` 已**取消/被后继取代**，**不**主张其 success）；其后 **D-171 一条决定条目**已随登记合并 `e023dbb` 推送 `origin/main`（`origin/main` 现为 `e023dbb`；该 head 同提交 CI run `35801778613` 于本次登记时**仍在进行中**，**不**主张其 success）；而 **D-172 一条决定条目**则由**本次尚未推送的登记提交**新增（**不在** `e023dbb` 中），故本条的已推送 head 与 CI 声明保持最近一次已推送状态（head `e023dbb`，其同提交 CI 结果以该 run 的实际终态为准）；**D-172** 登记了设备侧 D-158 §5 有效谓词代价读数（AVD `ul_p7_d01`、隔离 adb 5038、API 36、系统 SQLite 3.44.3、20,002 交易 / 40,004 posting 的 v31 库、`ANALYZE` 先行），带谓词 join 相对消融的 delta 约 **+17–22 ms**（相对约 +44–46%，消融侧约 41 ms）——该小且一致的绝对成本**远低于**用户可感知门限、**未**达到 D-158 §5 处置条件（索引边或另立决定）所针对的数量级回归，故 **D-158 §5 的设备前置条件已满足、处置条件未被触发**，P7-05 片 1b 的**设备前置条件据此解除**（片 1b **尚未开始**、仍待实施，除本读数外**无任何项记为 PASS**；该读数为写者单次运行值、不声称跨运行可复现性；D-167 的宿主侧读数与其限定**不变**），最高决定编号现为 **D-172**；四格式（微信 XLSX/支付宝 CSV/招行 CSV/建行 XLS）已双平台全可用（A-04.2 矩阵翻转，设备 instrumented 与产品路径证据背书；D01 披露式闭合与四格式可用性为两个不同命题）；A-05 导入全链验收为**部分完成**（D01～D06 逐项登记见「当前阶段」，开放项见「未完成门槛」）；A-01/A-02 设备验收已整体 PASS，A-03 部分完成（TB1～TB7 TalkBack 朗读巡检接受延期、A03-8b 缺证，见「未完成门槛」）；P7-05 修错与回收站设计门已按 D-156 批准、首切片（片 1a 基底层）已按 D-158 交付至 schema v31，片 1b（产品接线）与 P7-06～09 待按批裁决推进；阶段 6 已按 D-142 收口（全部批次 D-133…D-141 交付并关闭）。
