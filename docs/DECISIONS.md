# 决定记录

## 编号与状态

重要决定使用稳定的 `D-xxx` 编号。旧决定被替换时保留原文，并明确指向新的决定编号；不通过删除历史来制造一致性。

## D-001 统一的 3-in-1 账本

**状态：** 已确认

记账、账单导入和对账审计共用同一正式账本。来源记录、候选和正式账目分层保存。

## D-002 开发顺序

**状态：** 已确认

先完成核心规则和真实案例验证，再完成导入与对账闭环，随后建立最小客户端和完善视觉体验。

## D-003 客户端策略

**状态：** 已确认

Android 是日常主客户端，Desktop 是批量工作台。两端保留最小可运行外壳并共享完整业务核心，平台系统能力和 UI 独立。

## D-004 本地优先与可选扩展

**状态：** 原则已确认

本地账本是事实来源。同步和 AI 默认关闭，不能成为手工记账、标准导入、余额、报表和对账的前置依赖。
